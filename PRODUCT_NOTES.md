# Product notes

## Naming ideas

- `RIG ALIGN`을 향후 `STITCH`로 바꾸는 안을 검토한다.
- 현재는 기능 의미가 달라 변경하지 않는다. `RIG ALIGN`은 카메라 회전 보정이고,
  실제 스티칭 프리뷰는 `PREVIEW`, 전체 출력은 `RENDER`가 담당한다.
- 변경할 때는 버튼명만 바꾸지 말고 전체 작업 순서와 설명 문구를 함께 정리한다.
